import styled from 'styled-components';

export const VoyagerContainer = styled.div`
  position: relative;
  height: 100vh;
  background: black;
`;

export const ControllerContainer = styled.div`
  display: flex;
  position: absolute;
  top: 0;
  left: 0;
`;

export const ButtonContainer = styled.div`
  justify-content: space-between;
  margin: .2rem;
`;